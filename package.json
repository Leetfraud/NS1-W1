{
  "name": "nexiosol-site",
  "version": "1.0.0",
  "private": true,
  "description": "NexioSol studio website — static frontend + Vercel serverless API + MongoDB lead storage.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "mongodb": "^6.8.0",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
